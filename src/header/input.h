#ifndef GETINPUT_H
#define GETINPUT_H

/*
	GetInt
		Prompts the user with the string passed to the function and 
		returns an integer.
		
*/
int GetInt(char*);
char GetChar(char*);

int YNCharCheck(char);

#endif